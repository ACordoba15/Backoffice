export type RecordTS = {
    id: number;
    username: string;
    action: string;
    createdAt: string;
    updatedAt: string;
}

export type RecordGO = {
    ID: number;
    Username: string;
    Action: string;
    CreatedAt: string;
    UpdatedAt: string;
}